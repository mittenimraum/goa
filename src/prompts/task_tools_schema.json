{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "tool": {
      "type": "string",
      "enum": [
        "none",
        "web_search"
      ]
    },
    "query": {
      "type": "string",
      "description": "Search query if tool is web_search, otherwise empty string"
    },
    "reason": {
      "type": "string",
      "enum": [
        "general_knowledge",
        "needs_current_information",
        "needs_specific_comparison",
        "needs_external_reference"
      ]
    }
  },
  "required": [
    "tool",
    "query",
    "reason"
  ]
}