{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "tool": {
      "type": "string",
      "enum": [
        "none",
        "web_search"
      ]
    },
    "query": {
      "type": "string",
      "description": "Search query if tool is web_search, otherwise empty string"
    },
    "reason": {
      "type": "string",
      "enum": [
        "general_knowledge",
        "needs_current_information",
        "needs_specific_comparison",
        "needs_external_reference"
      ],
      "description": "The reason for the selected tool. Use general_knowledge for 'none'."
    }
  },
  "required": [
    "tool",
    "query",
    "reason"
  ]
}