{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "solution": {
      "type": "string",
      "description": "The written solution for the task in the users language and in markdown"
    },
    "summary": {
      "type": "string",
      "description": "A short summary of the solution in the users language"
    }
  },
  "required": [
    "solution",
    "summary"
  ]
}